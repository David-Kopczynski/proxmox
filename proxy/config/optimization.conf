# Optimization settings
# These can also prevent some attacks
gzip                    on;
gzip_vary               on;
server_tokens           off;

sendfile                on;
tcp_nopush              on;
tcp_nodelay             on;

http2                   on;
http3                   on;
quic_retry              on;

keepalive_timeout       75;
